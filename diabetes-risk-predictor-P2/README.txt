This project uses a diabetes dataset from Kaggle to predict whether a patient is diabetic or not using machine learning. 
The data was cleaned, balanced, preprocessed, and used to train three classification models: KNN, Logistic Regression, and Decision Tree. 
The models were evaluated using Accuracy and F1 Score.